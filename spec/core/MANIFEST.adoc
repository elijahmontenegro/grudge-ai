= Spidey Core Library
:project-name: Spidey
:module-name: Core Library
:lang: Go 1.26+
:codegen: protoc, go generate

== Context

=== Identity

*Name:* {project-name} {module-name}

*Paradigm:* Provider-Agnostic Abstraction Library

The core library is the foundational Go module. It defines the proto truth types, abstraction interfaces, and provider/embedder/classifier adapters that all other modules consume. It has no opinion on RRC, orchestration, UI, or deployment.

=== Stack

*Language:* {lang}

*Code Generation:* {codegen}

*Truth Types:* Protocol Buffers (`.proto` files are the canonical source; Go types, GraphQL schema fragments, and TypeScript types are all generated downstream)

*Dependencies:* Minimal. Standard library where possible. No framework dependencies.

=== Scope

==== In Scope

- Proto truth types: Message, Conversation, Role, Embedding, and related structures
- LLM interface: completion, streaming completion
- Embedder interface: single embed, batch embed
- Classifier interface: pairwise text classification (cross-encoder pattern -- takes two texts, returns class probabilities)
- Provider interface: lifecycle, capability discovery, configuration
- Provider adapters: Ollama, Anthropic, OpenAI, Google AI, vLLM, any OpenAI-compatible endpoint
- Embedder adapters: TEI, OpenAI embeddings
- Classifier adapters: TEI rerank/predict endpoint, custom inference endpoints
- Code generation pipeline: proto -> Go types, proto -> downstream schema fragments

==== Out of Scope

- RRC engine or algorithm (belongs to `spec/rrc`)
- Agent orchestration, agent loop, planning (belongs to `spec/service`)
- HTTP server, proxy, or service concerns (belongs to `spec/service`)
- UI, GraphQL schema, or presentation (belongs to `spec/web`)
- Conversation storage or persistence (belongs to `spec/service`)

== Implementation

// Implementation sections to be specified:
//
// - Proto Truth Types (.proto definitions: Message, Conversation, Role, Embedding, etc.)
// - LLM Interface (Completer: complete, stream, capability reporting)
// - Embedder Interface (Embedder: single, batch, dimensionality)
// - Classifier Interface (Classifier: pairwise text classification, returns class probabilities.
//   Covers cross-encoder reranking, NLI classification, discourse relation classification,
//   dependency detection. Provider-agnostic -- the RRC engine uses this without knowing
//   whether the backend is TEI, a custom model server, or an API.)
// - Provider Interface (Provider: lifecycle, config, capability discovery)
// - Provider Adapters (Ollama, Anthropic, OpenAI, Google AI, vLLM, OpenAI-compatible)
// - Embedder Adapters (TEI, OpenAI embeddings)
// - Classifier Adapters (TEI rerank/predict, custom inference endpoints)
// - Code Generation Pipeline (proto -> Go, proto -> downstream)
