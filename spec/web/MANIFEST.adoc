= Spidey Web UI
:project-name: Spidey
:module-name: Web UI
:rrc-full: Retrieval-Restored Continuation
:ui-framework: React 19
:api-protocol: GraphQL
:api-client: Apollo Client
:codegen: graphql-codegen

== Context

=== Identity

*Name:* {project-name} {module-name}

*Paradigm:* RRC-Native Agentic Interface

The web UI is a {ui-framework} application served by the service at `spidey.localhost`. The browser opens automatically when the service launches. It is the agentic interface -- not a chat clone.

Three things make this different from every other agentic UI:

1. **Citations.** Every message carries its RRC selection metadata -- which prior messages were selected as prerequisites, their scores, which thread they came from, whether the cross-encoder or QUD found them. The user sees what informed every response.
2. **Threads, not conversations.** Each thread is its own corpus with its own DAG, QUD graph, and carry-forward trajectory. Selection is thread-scoped by default, widened to all threads per-prompt. Each thread develops its own identity through accumulated carry-forward.
3. **Agentic surface.** Tool visualization, agent state, execution traces. The user sees what the agent is doing, why, and can intervene.

=== Stack

*UI Framework:* {ui-framework}

*Component Primitives:* shadcn/ui (Radix UI + Tailwind CSS, source-owned — copied into project, not installed as dependency)

*Design System:* Atomic design (atoms, molecules, organisms, templates, pages)

*API Client:* {api-client}

*Code Generation:* {codegen} ({api-protocol} schema -> TypeScript types)

*Served by:* `spec/service` (static assets at `spidey.localhost`)

*Depends on:* `spec/service` ({api-protocol} API)

=== Scope

==== In Scope

- Thread model: create, name, switch, delete threads. No "conversations" -- threads are the organizational unit.
- Per-prompt scope control: thread-scoped selection by default, toggle to all-threads for a specific prompt
- Citations per message: every message that triggered an RRC selection event carries citation metadata (selected prerequisites, scores, thread origin, cross-thread indicator)
- RRC introspection: drill into citations to see full selection detail, QUD graph state, carry-forward history
- Agentic interface: tool visualization, agent state, execution traces, planning views
- Message display: rich content (text, code, tool results, images) -- not a plain chat list
- Home page: landing view when opening Spidey
- {api-client} integration: queries, mutations, subscriptions against the service {api-protocol} API
- Code generation: {api-protocol} schema -> TypeScript client types via {codegen}
- UX design: interaction patterns, layout, information architecture
- Autonomous mode controls: duration picker, play/pause/stop, progress display, thread state in sidebar
- Build output: static assets (HTML/JS/CSS) served by the service

==== Out of Scope

- Backend logic (belongs to `spec/service`)
- Agent orchestration or tool execution (belongs to `spec/service`; this is the visualization surface)
- Thread storage (belongs to `spec/service`; the UI reads/writes via {api-protocol})
- RRC engine or algorithm (belongs to `spec/rrc`; the UI displays results)
- Standalone server or hosting (served by the service)
- Mobile layout

== Implementation

// Implementation sections to be specified:
//
// --- Thread Model ---
// - Thread list (home page sidebar or main view)
// - Create / rename / delete threads
// - Thread switching
// - Per-thread identity emergence (visible through carry-forward history and selection patterns)
//
// --- Citations ---
// - Citation indicator per message (count of selected prerequisites)
// - Expand to see: message preview, score, source (cross-encoder / QUD / both), thread origin
// - Cross-thread citations highlighted (selected from a different thread)
// - Outbound indicator: "referenced by N later messages" (shows which messages this one informed)
//
// --- Per-Prompt Scope Control ---
// - Scope toggle on input: thread-scoped (default) or all-threads
// - Visual indicator of current scope
//
// --- RRC Introspection (drill-down from citations) ---
// - Selection detail: full ranked list, scores, traversal path through DAG
// - Score breakdown per edge: cross-encoder score, QUD edge weight, temporal proximity
// - Exclusion rationale: why specific messages were not selected (below threshold, transitive reduction, depth limit)
// - QUD graph view: open/resolved QUDs, which messages establish and address each, tree hierarchy
// - Carry-forward view: accumulated reasoning, QUD enrichment history
// - DAG neighborhood view: for a given message, its prerequisites and dependents (ego graph, not full DAG)
//
// --- Agentic Interface ---
// - Tool visualization (tool definitions, in-flight executions, results, errors)
// - Execution traces (step-by-step agent reasoning and action history)
// - Agent state (current plan, progress, decision points, intervention surface)
//
// --- Thread View (main area when inside a thread) ---
// - Single column message stream, vertical scroll
// - Tool executions and thinking blocks expand/collapse inline (standard agentic pattern)
// - Citation indicator per message — expand inline to see RRC selection detail
// - Streaming: tokens arrive in real time via subscription
// - Rich content: text, code blocks, tool results, images
//
// --- Layout ---
// - Persistent sidebar (always visible): thread list with state indicators (idle, running, paused)
// - Main area changes based on context: home feed when no thread selected, thread view when inside a thread
// - Sidebar shows: thread name, last activity, autonomous state (running 47m, paused, idle)
//
// --- Home Page (main area when no thread selected) ---
// - Activity feed: recent cross-thread citations, completed autonomous runs, thread warmth
// - Quick start: prompt input that creates a new thread and sends in one action
// - New thread button
//
// --- Input ---
// - Two modes:
//   - Normal: prompt input, send button
//   - Autonomous: duration picker (5m, 30m, 2h, 8h, up to 2w), play/pause/stop controls
// - Scope toggle (thread / all-threads) — available in both modes
// - Mode switching (normal <-> autonomous) — input area transforms
// - Thread context indicator
//
// --- Autonomous Mode UI ---
// - Duration picker (microwave-style — user picks how long the agent runs)
// - Play: start autonomous execution
// - Pause: halt execution, user reviews and course corrects, resume continues
// - Stop: end autonomous mode
// - Progress: elapsed time, round count, current activity
// - Thread list shows running/paused state for autonomous threads
//
// --- UI Architecture ---
// - shadcn/ui — source-owned Radix UI + Tailwind primitives. Copied into project, modified directly. These are the atoms.
// - Strict atomic design: atoms (Button, Badge, Input), molecules (CitationIndicator, ThreadListItem, DurationPicker), organisms (MessageStream, Sidebar, InputArea), templates (ThreadView, HomeView), pages (Home, Thread, Settings)
// - Component structure: each component is self-contained, composable, no prop drilling beyond one level
// - {api-client} setup (queries, mutations, subscriptions, cache strategy)
// - Code generation ({codegen} config, generated TypeScript types)
// - Build pipeline (static asset output for service to serve)
