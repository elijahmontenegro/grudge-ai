= Grudge Web UI
:project-name: Grudge
:module-name: Web UI
:rrc-full: Retrieval-Restored Continuation
:ui-framework: React 19.2.4
:api-protocol: GraphQL
:api-client: Apollo Client 4.1.6
:codegen: graphql-codegen v6.2.1
:codegen-plugins: @graphql-codegen/typescript + @graphql-codegen/typescript-operations (types only — Apollo Client 4 hooks used directly)
:build-tool: Vite 8.0.5
:css-framework: Tailwind CSS 4.2.2
:css-vite-plugin: @tailwindcss/vite 4.2.2
:router: React Router 7.14.0
:typescript: TypeScript 6.0
:radix: radix-ui 1.4.3

== Context

=== Identity

*Name:* {project-name} {module-name}

*Paradigm:* RRC-Native Agentic Interface

The web UI is a {ui-framework} application served by the service at `grudge.localhost:8420`. The browser opens automatically when the service launches. It is the agentic interface -- not a chat clone.

Three things make this different from every other agentic UI:

1. **Citations.** Every message carries its RRC selection metadata -- which prior messages were selected as prerequisites, their scores, which thread they came from, whether the cross-encoder or QUD found them. The user sees what informed every response.
2. **Threads, not conversations.** Each thread is its own corpus with its own DAG, QUD graph, and carry-forward trajectory. Selection is thread-scoped by default, widened to all threads per-prompt. Each thread develops its own identity through accumulated carry-forward.
3. **Agentic surface.** Tool visualization, agent state, execution traces. The user sees what the agent is doing, why, and can intervene.

=== Stack

*UI Framework:* {ui-framework}

*Component Primitives:* shadcn/ui (Radix UI + {css-framework}, source-owned -- copied into project, not installed as dependency)

*Build Tool:* {build-tool}

*Design System:* Atomic design (atoms, molecules, organisms, templates, pages)

*API Client:* {api-client}

*Language:* {typescript}

*Routing:* {router}

*CSS Plugin:* {css-vite-plugin}

*Radix Primitives:* {radix} (unified package)

*Code Generation:* {codegen} — plugins: {codegen-plugins}

*Served by:* `spec/service` (static assets at `grudge.localhost`)

*Depends on:* `spec/service` ({api-protocol} API)

=== Scope

==== In Scope

- Thread model: create, name, switch, archive, delete threads. No "conversations" -- threads are the organizational unit.
- Thread branching: edit a message creates a branch (fork). Sidebar shows branch relationships. Navigate between branches.
- Semantic search: search across all threads, results ranked by embedding similarity
- Command palette: keyboard-accessible (Cmd+K / Ctrl+K) for skill invocation, thread switching, search, settings. Primary capability discovery mechanism.
- Per-prompt scope control: thread-scoped selection by default, toggle to all-threads for a specific prompt
- Citations per message: every message that triggered an RRC selection event carries citation metadata (selected prerequisites, scores, thread origin, cross-thread indicator)
- RRC introspection: drill into citations to see full selection detail, QUD graph state, carry-forward history
- Agentic interface: tool visualization, agent state, execution traces
- Message display: rich content (text, code, tool results, images) -- not a plain chat list
- Home page: landing view when opening Grudge
- Autonomous mode controls: duration picker, play/pause/stop, progress display, thread state in sidebar
- {api-client} integration: queries, mutations, subscriptions against the service {api-protocol} API
- Code generation: {api-protocol} schema -> TypeScript client types via {codegen}
- Build output: static assets (HTML/JS/CSS) served by the service

==== Out of Scope

- Backend logic (belongs to `spec/service`)
- Agent orchestration or tool execution (belongs to `spec/service`; this is the visualization surface)
- Thread storage (belongs to `spec/service`; the UI reads/writes via {api-protocol})
- RRC engine or algorithm (belongs to `spec/rrc`; the UI displays results)
- Standalone server or hosting (served by the service)
- Mobile layout

== User Stories

=== Thread Management

- User opens Grudge -> sees thread list with state indicators (idle, running, paused) and warmth (carry-forward count) -> picks a thread or creates one
- User creates a thread -> types a name or starts with a prompt (thread auto-named from first prompt) -> adds one or more working directories
- User adds or removes mounted directories from a thread at any time
- User sees which threads are running/paused/idle from any view via the persistent sidebar
- User archives a thread -> thread moves to archive view, all state preserved, accessible later
- User deletes a thread -> explicit warning ("this is irreversible, all data will be wiped") -> hard delete
- Sidebar shows branch relationships (parent -> children) for branched threads

=== Message Editing (Branching)

- User clicks edit on a previous user message -> input area appears with message content
- User modifies the message -> sends -> a new branch is created from that point
- Old thread unchanged, new branch appears in sidebar as a child
- User navigates between branches -> each branch has its own identity and warmth
- No message deletion. Don't want a message? Branch from before it.

=== Search

- User opens command palette (Cmd+K / Ctrl+K) -> types search query
- Results ranked by semantic similarity across all threads
- Each result shows message snippet, thread name, score
- User clicks a result -> navigates to that message in its thread

=== Command Palette

- User presses Cmd+K / Ctrl+K -> command palette opens
- Available actions: search, invoke skill, switch thread, open settings, enter plan mode
- Fuzzy matching on action names and skill names
- Primary capability discovery mechanism for the web UI

=== Normal Interaction

- User types a prompt -> chooses scope (thread or all-threads) -> sends
- Agent works -> user sees streaming response, tool executions and thinking blocks expand/collapse inline
- User stops the agent mid-execution -> agent halts, partial work preserved in thread

=== Citations

- User sees citation count on every message that went through an RRC selection event
- User expands citations inline -> sees listed prerequisites with score, source (cross-encoder / QUD / both), thread origin
- User clicks a cited message -> navigates to it (scrolls within thread or jumps to other thread)
- User sees outbound indicator on messages -> "referenced by N later messages"
- Cross-thread citations are visually distinct from same-thread citations

=== Autonomous Mode

- User switches to autonomous mode -> duration picker appears
- User types initial prompt -> sets duration (5m to 2w) -> hits play
- Agent runs -> user sees progress (round count, elapsed time, current activity)
- User pauses -> input area appears for course correction -> types correction -> resumes
- User stops -> agent halts -> thread shows final state
- Timer expires -> agent stops naturally
- User can navigate to other threads while autonomous agent runs -- progress visible in sidebar

=== Subagents

- Parent model spawns a subagent -> user sees the task and a progress indicator
- Subagent works in the background -> user can navigate to other threads, subagent progress visible in sidebar
- Subagent finishes -> result appears inline in parent thread
- User expands to see the subagent's intermediate work (tool calls, reasoning, file reads) in a collapsible sub-view
- Multiple subagents can run concurrently (ParallelAgent) -> each shows progress independently
- User can message a running subagent (SendMessage) -> course correction without stopping it

=== Plan Mode

- User requests a plan -> agent enters plan mode (read-only), explores codebase, writes a plan as an AsciiDoc document
- UI surfaces the plan for review -- user reads, edits the text, approves or rejects
- On approval -> agent exits plan mode (write tools restored), implements according to the plan
- The plan enters the thread as a corpus message on approval. RRC selects it as a prerequisite during execution -- the agent doesn't lose the plan across rounds.
- The plan is a design artifact, not a step tracker. Progress is implicit in the work being done.

=== Tool Permissions

- Agent requests a write tool (file edit, bash) -> user sees pending tool call with details -> approves or denies
- User configures per-thread permission defaults (allow/ask/deny per tool category)
- Denied tool call -> agent sees denial, adjusts approach or asks user for clarification

=== Cross-Thread

- User toggles scope to all-threads for a specific prompt -> agent draws from all threads
- User sees cross-thread citations highlighted distinctly in citation expansion
- User navigates from a cross-thread citation to the source thread and back without losing state

=== First Run

- User launches Grudge for the first time -> service starts, tray icon appears, browser opens
- Onboarding: user provides name and preferences (stored once, injected into prompt templates)
- Service probes localhost for Ollama (default port) and TEI (default port)
- Detected -> shows detected providers (LLM, cross-encoder, small fast model), user confirms
- Not detected -> setup form: endpoint URLs, model selection, API key for cloud providers
- All three model roles configured -> home page loads -> ready

=== Errors

- LLM provider unreachable -> error shown in thread, agent pauses, user can retry or reconfigure in settings
- Cross-encoder (TEI) unreachable -> system does not work. RRC is not optional. Error displayed, links to settings, system waits until resolved. No degraded mode, no passthrough.
- Autonomous run fails mid-execution -> agent pauses with error visible, user reviews, retries or stops
- Service restarts while autonomous agent is running -> agent state shows last known state on reconnect

=== Introspection (drill-down from citations)

- User expands a citation -> sees score breakdown (cross-encoder, QUD, temporal proximity)
- User views the QUD graph state at the time of that selection event
- User sees what the model actually received (assembled payload) vs the full thread

== Prerequisites

These are not features. They are conditions the user stories depend on.

=== State Persistence Across Navigation

Thread view state survives navigation -- scroll position, expanded/collapsed sections, input draft. Leave and come back, it's the same. Managed by the service, not the browser. Tab close and reopen restores it.

=== Service Restart Resilience

If the service restarts, all persistent state survives (threads, messages, DAG, scores, QUD graphs in SQLite). View state is reconstructable. Running autonomous agents resume or show their last state. The browser reconnects automatically.

=== Concurrent Thread Awareness

Multiple threads can be active simultaneously. Navigating between threads does not affect running agents. The sidebar reflects all thread states in real time regardless of which thread the user is in.

=== Real-Time Updates Across Views

Agent progress updates in the sidebar even when the user is in a different thread. Cross-thread citations appearing in the home activity feed while the user is in a thread. All via {api-protocol} subscriptions.

=== Navigation History

Citation jumps are navigation events. Browser back/forward work naturally. `grudge.localhost/thread/{id}` is a real URL. Back returns to the same scroll position and expanded state.

=== Offline Resilience

If the browser disconnects (network blip, tab backgrounded), it reconnects and rehydrates from the service. No data loss. The service is the source of truth.

=== Input Draft Persistence

User starts typing, navigates away, comes back -- draft is still there. Per-thread.

== Implementation

=== Layout

Persistent sidebar (always visible): thread list with state indicators (idle, running, paused, with elapsed time for running threads). Main area changes based on context: home feed when no thread selected, thread view when inside a thread.

=== Home Page

Main area when no thread selected. Activity feed: recent cross-thread citations, completed autonomous runs. Quick start: prompt input that creates a new thread and sends in one action. New thread button.

=== Thread Warmth

Count of carry-forward events in a thread -- how many LLM responses have fed back into the thread's QUD graph. A thread with 0 is cold (cross-encoder edges only, no structural understanding). A thread with 200 is warm (rich QUD graph, strong identity). Displayed as a subtle indicator in the sidebar per thread and in the home activity feed.

=== Thread View

Single column message stream, vertical scroll. Tool executions and thinking blocks expand/collapse inline (standard agentic pattern). Citation indicator per message -- expand inline to see RRC selection detail. Streaming tokens arrive in real time via subscription. Rich content: text, code blocks, tool results, images.

=== Citations

Citation indicator per message: count of selected prerequisites. Expand inline to see: message preview, score, source (cross-encoder / QUD / both), thread origin if cross-thread. Cross-thread citations visually distinct. Click a cited message to navigate to it. Outbound indicator: "referenced by N later messages."

=== Introspection

Drill-down from citation expansion. Selection detail: full ranked list, scores, traversal path through DAG. Score breakdown per edge: cross-encoder score, QUD edge weight, temporal proximity. Exclusion rationale: why specific messages were not selected (below threshold, transitive reduction, depth limit). QUD graph view: open/resolved QUDs, which messages establish and address each. Carry-forward view: accumulated reasoning. DAG neighborhood: for a given message, its prerequisites and dependents.

=== Input Area

Two modes driven by user's cognitive state:

**Idle:** Text area, send button, scope toggle (thread / all-threads), mode switch (normal / autonomous).

**Autonomous setup:** When autonomous mode selected, duration picker appears (5m to 2w). Text area stays for the initial prompt. Send becomes play.

**Agent working (normal):** Stop button appears. Text area available for interruption.

**Agent working (autonomous):** Pause + stop. Duration countdown. Progress: round count, elapsed time, current activity.

**Agent paused (autonomous):** Text area returns for course correction. Resume + stop. User types correction, hits resume, correction enters the thread, agent continues.

=== Settings

Minimal. The system works without configuration when providers are detected at default ports. Configuration required on first run:

- Provider endpoint (supported: Ollama, Anthropic, OpenAI/compatible, Google AI, vLLM, TEI)
- Model selection
- API key (for cloud providers)
- Scorer endpoint (where is the cross-encoder? TEI at localhost, etc.)

Smart detection: if Ollama and TEI are running at default localhost ports, detect and offer them. Reduce first-run friction to confirmation, not configuration.

Per-thread settings: mounted directories (add/remove), sandbox on/off, tool permissions (allow/ask/deny per tool).

Accessible from tray menu ("Settings...") and from within the web UI.

=== UI Architecture

shadcn/ui source-owned primitives built on the unified `radix-ui` package + {css-framework}. Copied into project, modified directly. {css-framework} uses CSS-first configuration via `@theme` directives in CSS -- no `tailwind.config.ts`. Strict atomic design methodology: atoms compose into molecules, molecules into organisms, organisms into templates, templates into pages. Each component self-contained and composable.

{api-client} for all service communication: queries, mutations, subscriptions. Generated TypeScript types from {api-protocol} schema via {codegen}. Static asset build output served by the service.

=== File Tree

----
web/
├── package.json
├── index.html                    # Vite entry point (<script type="module" src="/src/main.tsx">)
├── tsconfig.json                 # TypeScript config (strict, JSX, path aliases)
├── vite.config.ts
├── codegen.ts                    # graphql-codegen config ({codegen-plugins})
├── dist/                         # Build output (served by service)
├── src/
│   ├── main.tsx                  # Entry point
│   ├── App.tsx                   # Root layout, router, Apollo provider
│   ├── graphql/
│   │   ├── queries.ts            # GraphQL query documents
│   │   ├── mutations.ts          # GraphQL mutation documents
│   │   ├── subscriptions.ts      # GraphQL subscription documents
│   │   └── generated/            # graphql-codegen output (TypeScript types)
│   ├── components/
│   │   ├── ui/                   # shadcn/ui primitives (source-copied)
│   │   ├── atoms/                # Smallest composable units
│   │   ├── molecules/            # Composed atoms
│   │   ├── organisms/            # Complex composed components
│   │   └── templates/            # Page-level layout compositions
│   ├── pages/
│   │   ├── Home.tsx              # Home feed, quick start
│   │   ├── Thread.tsx            # Thread view, message stream
│   │   └── Settings.tsx          # Provider config, permissions
│   ├── hooks/                    # React hooks (subscriptions, state)
│   └── lib/                      # Utilities, Apollo client config
└── app.css                       # Tailwind v4 entry: @import "tailwindcss" + @theme directives
----

=== Build Tooling

{build-tool}. Output to `web/dist/`. The service serves static files at `/` with SPA fallback to `index.html`. Dev workflow: Vite dev server proxying `/{api-protocol}` to the service.

=== TypeScript Types

All domain types generated from {api-protocol} schema via {codegen}. No hand-defined TypeScript domain types. Component props use generated types directly. Output in `src/graphql/generated/`.

=== {api-protocol} Operations Consumed

Operations reference the service spec's {api-protocol} schema (`spec/service`).

==== Home Page

- `threads(includeArchived: Boolean): [Thread]`
- `createThread(name: String, workingDirs: [String!]): Thread`
- `search(query: String!, limit: Int): [SearchResult]`
- `threadStateChanges: ThreadStateEvent` (subscription)

==== Command Palette

- `search(query: String!, limit: Int): [SearchResult]`
- `threads(includeArchived: Boolean): [Thread]` (for thread switching)

==== Thread View

- `messages(threadId: ID!, limit: Int, offset: Int): [Message]`
- `messageStream(threadId: ID!): StreamEvent` (subscription)
- `sendMessage(threadId: ID!, content: String!, scope: SelectionScope): Message`
- `stopAgent(threadId: ID!): Boolean`
- `pauseAgent(threadId: ID!): Boolean`
- `resumeAgent(threadId: ID!, correction: String): Boolean`
- `startAutonomous(threadId: ID!, prompt: String!, duration: String!): Boolean`
- `editMessage(threadId: ID!, messagePosition: Int!, newContent: String!): Thread` (returns new branch thread)
- `archiveThread(id: ID!): Boolean`
- `deleteThread(id: ID!): Boolean`
- `approveToolCall(callId: ID!): Boolean`
- `denyToolCall(callId: ID!, reason: String): Boolean`
- `agentState(threadId: ID!): AgentState` (subscription)
- `toolExecution(threadId: ID!): ToolExecution` (subscription)
- `subagentProgress(threadId: ID!): SubagentProgress` (subscription)

==== Citations / Introspection

- `selectionResult(eventId: ID!): SelectionResult` (proto type via gqlgen model binding)
- `qudGraph(threadId: ID!): QUDGraph` (proto type via gqlgen model binding)

==== Settings

- `settings: Settings`
- `updateSettings(input: SettingsInput!): Settings`

==== View State

- `viewState(threadId: ID!): ViewState`
- `saveViewState(threadId: ID!, state: ViewStateInput!): ViewState`

All types (`SearchResult`, `StreamEvent`, `AgentState`, `ToolExecution`, `SubagentProgress`, `ThreadStateEvent`, `ViewStateInput`, `SettingsInput`, `SelectionScope`, `ExecutionMode`) are defined in `spec/service` GraphQL Type Definitions section.

=== Error Handling

Errors display inline in the message stream (not modal or toast) -- errors are thread-scoped. Subscription disconnection: reconnect with exponential backoff (1s initial, 30s max). {api-protocol} errors: display `error.message` in thread. Network errors: display connection status banner, auto-retry subscription.

=== View State Persistence

`ViewState` type:

[source,typescript]
----
interface ViewState {
  scrollPosition: number;
  expandedMessageIds: string[];
  inputDraft: string;
  citationExpansionState: Record<string, boolean>;
}
----

Persisted via `saveViewState` mutation, debounced 500ms after change + save on navigation. Restored on thread switch via `viewState` query. The service is the source of truth -- tab close and reopen restores full view state.

=== Thread Warmth

Warmth is unbounded (count of carry-forward events). 0 = cold (cross-encoder edges only, no structural understanding). Display: numeric count in sidebar per thread. Visual indicator intensity scales logarithmically -- the difference between 0→10 is more visually significant than 100→110.

=== Autonomous Duration

5m minimum: shortest useful autonomous exploration. 2w maximum: practical upper bound for unattended local workstation operation. Text field accepting `<number><unit>` where unit is one of `m` (minutes), `h` (hours), `d` (days), `w` (weeks). Compound durations (e.g., `1h30m`) not supported — use minutes (`90m`). Parsed by the service.

=== Example Data

==== Message with Citations Expanded

[source,json]
----
{
  "id": "msg-5",
  "role": "ROLE_USER",
  "content": [{"text": "Does that apply to the initial OAuth flow too?"}],
  "citations": {
    "count": 4,
    "selected": [
      {
        "message_id": "msg-4",
        "preview": "Token refresh retries 3 times with exponential backoff: 1s, 2s, 4s.",
        "effective_score": 0.87,
        "source": "cross-encoder",
        "thread_origin": "thread-auth",
        "cross_thread": false
      },
      {
        "message_id": "msg-2",
        "preview": "I'll set up the OAuth flow with PKCE...",
        "effective_score": 0.73,
        "source": "cross-encoder",
        "thread_origin": "thread-auth",
        "cross_thread": false
      }
    ]
  }
}
----

==== Thread List with Warmth

[source,json]
----
[
  {"id": "thread-auth", "name": "Auth Module", "state": "running", "warmth": 47, "elapsed": "12m"},
  {"id": "thread-api", "name": "API Design", "state": "idle", "warmth": 203},
  {"id": "thread-new", "name": "New Thread", "state": "idle", "warmth": 0}
]
----

==== Search Result

[source,json]
----
{
  "messageId": "msg-42",
  "threadId": "thread-api",
  "threadName": "API Design",
  "snippet": "...the retry policy uses exponential backoff with a 3-attempt maximum...",
  "score": 0.91
}
----

==== Introspection Drill-Down (Score Breakdown)

[source,json]
----
{
  "messageId": "msg-4",
  "effectiveScore": 0.87,
  "breakdown": {
    "crossEncoderScore": 0.94,
    "qudWeight": 0,
    "temporalProximity": 0.5,
    "fused": "0.7*0.94 + 0.2*0 + 0.1*0.5 = 0.708"
  },
  "exclusions": [
    {"messageId": "msg-1", "reason": "TRANSITIVE_REDUCTION", "score": 0.51}
  ]
}
----

==== Autonomous Progress

[source,json]
----
{
  "threadId": "thread-auth",
  "status": "RUNNING",
  "mode": "AUTONOMOUS",
  "roundCount": 7,
  "startedAt": "2026-04-06T16:00:00Z",
  "durationLimit": "2h",
  "elapsedTime": "12m"
}
----

==== Subagent Progress

[source,json]
----
{
  "threadId": "thread-auth",
  "forkThreadId": "thread-auth-fork-1",
  "task": "Research existing OAuth implementations in the codebase",
  "status": "running",
  "roundCount": 3
}
----
